## tesselar homelab

### tess cli

CLI to manage homelab clusters and applications. Self documented, use `tess --help` to explore it.

### git projects

when working on tesselar, you will likely need to work across the following git projects:

- `~/SyncPersonal/projects/tesselar` - app code
- `~/SyncPersonal/projects/homelab-infra` - persisted app data and cluster configuration

## chezmoi dotconfig

`~/.local/share/chezmoi`
