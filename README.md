## 🚀 One-Line Installation

Open your terminal and paste the following command. This will download the bootstrap script, install Homebrew and chezmoi, and apply all configurations.

```
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/mauriciomelo/dotfiles/main/install.sh)"
```
